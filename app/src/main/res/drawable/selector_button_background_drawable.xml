<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled state -->
    <item android:drawable="@drawable/disabled_background_drawable" android:state_enabled="false" />
    <!-- Default state (enabled) -->
    <item android:drawable="@drawable/button_drawable_background" />
</selector>